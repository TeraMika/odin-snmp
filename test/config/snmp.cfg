[server]
debug_mode  = 1
http_port   = 8888
http_addr   = 192.168.0.22
adapters    = snmp
enable_cors = true

[tornado]
logging = debug

# adapter that makes requests at given interval
[adapter.snmp]
module = snmp.adapter.SnmpAdapter
